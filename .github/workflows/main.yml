name: GithubACtionTest

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/chekcout@v2
      - name: 파일 확인
        run: ls -al
      - name: 파이썬 실행
        run: python test.py
